# LazyShell Plan

本文件沉淀项目现阶段的实现分析、演进方向、架构判断和后续计划。面向使用者的安装、运行和操作说明请看根目录 [README.md](../README.md)。

## 项目简介

LazyShell 是一个基于 Tauri + React 的桌面终端应用，目标是把自然语言、安全确认和 SSH 运维能力组合到一个本地客户端里。用户可以通过 AI 将自然语言转换为 Linux 命令，在执行前查看命令内容，并通过 SSH 连接远程服务器完成操作。

它不是一个 Web 管理面板，而是一个偏“本地运维工作台”的产品：

- AI 负责理解意图并生成命令
- Rust 后端负责 SSH、加密和本地持久化
- React 前端负责多服务器、多标签、终端与 AI 协作界面

当前项目已从 Claude Code 阶段转入 Codex 继续开发，本文档作为统一的项目说明与开发入口，后续以 `README.md` 为主，不再让说明分散在多份文档里。

## 核心能力

- 自然语言转 Linux 命令
- 多 AI Provider 支持，运行时切换
- SSH 远程连接与命令执行
- 多标签页服务器管理
- 交互式终端体验
- 服务器配置本地加密存储
- 危险命令识别与二次确认
- 自动经验学习与会话记忆

## 技术栈

- 前端: React 19 + TypeScript + Vite
- 后端: Rust + Tauri 2.x
- 终端: xterm.js + xterm-addon-fit + xterm-addon-web-links
- SSH: `ssh2`
- 加密: AES-256-GCM + PBKDF2
- AI: MiniMax / OpenAI / Anthropic

## 当前已实现功能

## 最近更新

本轮开发重点放在 AI 输入区重构、聊天展示收口、聊天/历史数据一致性修复、OpenClaw 指令手册接入、远程文件可视化能力落地，以及 AI 后端自动学习链和服务器环境画像能力补齐。

- AI 输入区已从“普通聊天框”重构成更偏运维工作台的结构：
  - 模式切换移入输入区，使用下拉选择
  - 多行输入支持继续保留
  - AI 返回的单命令 / 多候选命令统一进入上方“待执行动作”卡
- 聊天界面表现已收口：
  - 聊天角色名称由 `AI` 改为 `智能助手`
  - 消息正文末尾不再拼接 `[AI]`、`[本地命令库]`、`[历史学习]` 这类尾巴
  - 来源改成消息头部 badge 展示
  - 多候选命令即使关闭上方执行单，也会在聊天记录中以结构化方式保留
  - AI 消息现已支持安全子集 Markdown 渲染，标题、列表、代码块、行内代码和链接都会正常展示
- AI 区组件边界已开始拆分：
  - `AIConversationPanel`
  - `AIHistoryPanel`
  - `AIFavoritesPanel`
  - `AIBuiltinPanel`
  - `AIPendingActionCard`
- 聊天历史一致性已补齐：
  - `sourceLabel` 现已持久化，重启后不会丢
  - 历史聊天恢复时会继续带回 `options`
  - AI 返回 fenced JSON / JSON 文本时，前后端都会尝试归一化解析，尽量避免原始 JSON 直接落入聊天正文
  - 聊天历史恢复与“上滑加载更早消息”现已按时间顺序一致，默认优先恢复最新一页
- 命令历史策略已收口：
  - 历史记录继续保留 AI、历史重跑、常用命令、内置命令、直连执行这些明确动作
  - 手动在终端中输入的命令不再写入命令历史，避免把不完整的编辑过程和试探性输入混进去
- AI 聊天区布局细节已优化：
  - 顶部无关上下文条已移除
  - 多候选执行卡片区域支持内部滚动
  - 执行卡片支持手动关闭，避免长期遮挡聊天区
  - 内置命令与 OpenClaw 指令二级分类默认折叠，避免长列表首次展开时压满面板
- OpenClaw 指令手册已接入 AI 面板：
  - `OPENCLAW` 现已成为与“内置”并列的一级标签
  - OpenClaw 指令不再混在 Linux 内置命令里
  - 页面内按命令作用拆成可折叠分类，例如基础入门、网关、配置管理、模型管理、频道管理、聊天斜杠命令等
  - 聊天斜杠命令卡片改为“复制”而不是直接执行到 shell，避免误发进终端
- 远程文件可视化能力已接入：
  - 终端标题栏支持直接打开独立的文件浏览器窗口
  - 文件浏览器基于 SSH2/SFTP，支持目录树、目录列表、返回上一级、刷新、上传、下载
  - 文本文件双击后会打开独立编辑器窗口，而不是嵌在终端区
  - 文件浏览器与文本编辑器均已切到偏 macOS 风格的深色模式
  - 长目录、长文件和编辑器内容都已支持滚动查看
  - 文本编辑器基于 CodeMirror 6，当前重点支持 JSON、Shell、YAML 高亮
 - AI 后端学习链已收口到 Rust：
  - 前端设置页中的手工“学习数据”入口已移除，不再要求用户自己维护 JSON 数据
  - AI 主链统一走后端 orchestrator，前端不再自行拼 learning 数据或本地记忆上下文
  - 执行反馈会自动沉淀为 execution experiences，并区分“AI 原样成功”和“用户修改后成功”
  - 后端会维护长期记忆与经验权重，用于下一轮 prompt 召回和候选方案排序
 - 服务器环境画像已接入：
  - 每台服务器会自动沉淀 `ServerEnvironmentProfile`
  - 当前会记录 hostname、OS、发行版、包管理器、默认 shell，以及 `docker`、`systemd`、`pm2`、`nginx`、`openclaw` 等能力标签
  - 画像来源包括状态采集、banner、执行反馈和按需轻量探测
  - 当用户问题明显依赖特定运行环境而画像缺失时，后端会先做安全只读 probe，例如 `command -v docker`、`command -v openclaw`

### AI 智能助手

- 自然语言转 Linux 命令，当前由后端 orchestrator 统一召回历史经验、长期记忆和近期上下文后再调用 AI Provider
- 多 Provider 支持，可在运行时切换
- AI 上下文注入：当前目录、最近命令、会话状态、命令卡片、聊天摘要、执行经验、长期记忆、服务器环境画像
- 多命令候选与澄清式响应
- 自动执行反馈学习、经验权重分层、长期记忆召回
- 支持按需轻量探测环境能力，在画像缺失时优先补齐运行环境线索

### 服务器与会话管理

- SSH 连接管理，支持密码和私钥认证
- 多标签页服务器管理
- 服务器欢迎横幅与系统信息展示
- 服务器状态监控：磁盘、内存、网络、监听进程
- Master Password 解锁流程

### 终端体验

- 主终端使用 xterm.js
- 样式化提示符 `username@hostname:path$`
- 上下箭头历史导航
- Tab 命令补全
- Ctrl+C / Ctrl+V 支持
- 接近 macOS Terminal 的默认视觉风格
- 终端标题栏支持直接打开远程文件浏览器独立窗口

### 远程文件浏览器与编辑器

- 基于 Tauri 子窗口实现独立文件浏览器与独立文本编辑器
- 文件浏览器支持树形目录、目录列表、进入文件夹、返回上一级、刷新
- 支持上传文件到当前远程目录、下载远程文件到本地
- 文本文件双击后进入独立编辑窗口
- 编辑器支持保存、未保存修改提示
- 编辑器支持 JSON / Shell / YAML 高亮，未知文本回退纯文本
- 文件浏览器与编辑器统一采用深色模式，视觉方向参考 macOS 深色界面

### 安全

- 服务器配置本地加密存储
- Master Password 驱动的解锁与密钥派生
- 命令预览确认
- 高危命令二次确认，例如 `rm -rf`、`shutdown`、`reboot`、`dd`、`mkfs`

### 命令库与记忆

- 内置 50+ Linux 命令数据库
- 命令按 `file`、`text`、`system`、`network`、`process`、`archive`、`disk`、`package` 分类
- 每个命令包含描述、参数、示例、使用场景
- OpenClaw 指令速查手册已独立接入，作为 AI 面板中的 `OPENCLAW` 一级标签存在
- OpenClaw 页面按命令作用分类展示，支持折叠收起，聊天斜杠命令与 CLI 命令分开处理
- 聊天历史与命令卡片的本地持久化
- 命令执行历史按服务器维度持久化
- 历史命令可在 AI 区域中搜索、筛选、去重和重跑
- 历史命令带来源标记，例如 AI / 终端 / 常用 / 内置 / 历史重跑
- 历史支持裁剪保留最近 50 / 100 / 300 条，以及清空历史
- 学习和记忆主链已转为后端自动沉淀，不再要求用户手动导入学习数据

## 已完成功能分析

这一节关注“代码里真正已经做成了什么”，不是仅根据功能清单描述。

### 1. 安全与本地存储

当前安全链路已经形成闭环：

- 首次启动可设置主密码
- 后续启动需解锁后才能访问服务器配置
- 服务器配置以本地加密文件形式持久化
- 删除服务器时会同步清理该服务器相关记忆数据

这意味着安全能力不是单纯的 UI 表单，而是已经落实到后端命令、解锁状态和本地文件存储流程里。

### 2. 服务器管理

服务器管理已经具备真实可用的基础能力：

- 新增服务器
- 编辑服务器
- 删除服务器
- 测试 SSH 连接
- 支持密码认证和私钥认证

前端表单和后端请求结构已经基本对齐，说明这块功能已从静态页面进入可执行状态。

### 3. SSH 与执行模型

当前后端已经同时具备两类执行能力：

- 普通命令执行
- 面向终端交互的持久 shell / PTY 能力

其中主线路已经不是“一次请求执行一条命令”这么简单，而是带有持续连接、输入输出、尺寸同步和交互命令支持的终端模型。这是项目完成度最高的核心模块之一。

### 4. 主终端体验

当前主终端功能已经超过基础回显：

- 基于 xterm.js 的真实终端视图
- 行列数同步到后端 shell
- 键盘输入透传
- 输出顺序轮询读取
- 对方向键等转义序列做兼容处理
- 支持链接识别

这说明终端体验层已经经历过实际调试，而不是只停留在“命令框 + 输出区”。

### 5. AI 助手能力

AI 部分已经具备产品化接口而不是简单调用 API：

- 多 Provider 抽象与切换
- 统一的 `AiDecision` 决策结构
- 支持单命令、多候选、澄清式响应
- 后端 orchestrator 统一完成模式判断、上下文召回、风险分级和 prompt 组装
- 上下文注入包含目录、近期命令、会话状态、执行经验、长期记忆和服务器环境画像

这使 AI 不只是“生成一行命令”，而是参与实际的运维交互流程。

### 6. 记忆与学习

当前知识沉淀已逐步统一到后端自动学习链路：

- execution experiences: AI 建议、最终执行命令、成功/失败、用户是否修改命令
- memory system: 聊天历史、命令卡片、命令执行历史、长期记忆、危险级别
- server environment profile: 服务器级运行环境画像、能力标签与证据来源
- 环境能力缺失时支持按需只读探测，而不是依赖用户手工上传学习数据

这套能力让项目开始具备“越用越像个人工作台”的特征，而不是一次性 AI 工具。

### 7. 命令历史工作流

当前命令历史已经不只是临时内存，而是形成了一条可复用链路：

- 显式执行命令后会持久化到本地
- AI 自动执行到终端的命令会进入历史
- 历史可在 AI 面板“历史”标签中搜索、筛选、去重和重跑
- 历史记录带来源标记，便于后续排查和行为分析
- 历史输出已做统一截断，避免单条超长输出快速撑大历史文件
- 历史支持保留最近若干条或清空当前服务器历史

### 8. 多标签与布局

当前主界面已经完成工作台形态的基础组织：

- 多服务器 tab
- 活动 tab 切换
- tab 关闭时清理 shell 状态
- 侧边栏 / 终端 / AI 区域布局切换
- AI 面板宽度拖拽

这说明产品形态已经不是单服务器单窗口，而是朝多会话运维场景在走。

## 用户操作路径分析

下面按真实使用流程梳理当前已经完成的用户旅程。

### 路径 1: 首次打开应用

1. 用户进入解锁页。
2. 若本地不存在主密码，则进入设置模式。
3. 设置主密码后应用解锁，进入主界面。
4. 后续服务器配置和敏感信息通过该密码派生的密钥进行本地保护。

这一段说明“首次使用”已经不是裸奔状态，而是从一开始就把安全流程纳入入口。

### 路径 2: 添加并验证服务器

1. 用户在服务器侧边栏打开添加表单。
2. 输入名称、主机、端口、用户名和认证方式。
3. 选择密码或私钥认证。
4. 保存后后端进行配置落盘。
5. 用户可立即发起连接测试，验证配置是否有效。

这条路径已经形成完整的配置管理闭环。

### 路径 3: 打开一个服务器会话

1. 用户在侧边栏选择某台服务器。
2. 前端先请求创建 shell session。
3. 仅当后端 shell 创建成功后，才把该服务器加入 tab。
4. 主终端接管这个 `tabId` 的输入输出。
5. 同时异步获取 welcome banner。

这条路径设计得比较稳，避免了“tab 已出现但后端 shell 不存在”的孤儿状态。

### 路径 4: 在终端中执行交互操作

1. 用户输入内容进入 xterm。
2. 输入通过 `shell_input` 发给后端持久 shell。
3. 前端通过顺序轮询持续读取 `shell_output`。
4. 输出写回 xterm，形成交互式终端体验。
5. resize 时同步行列数，尽量保证全屏程序行为正常。

这条路径说明主终端已经不是伪终端，而是围绕持久 shell 设计的。

### 路径 5: 使用 AI 生成命令

1. 用户在 AI 面板输入自然语言需求。
2. 前端把请求、当前上下文和服务器信息交给后端 orchestrator。
3. 后端统一召回命令卡片、执行经验、长期记忆和服务器环境画像；必要时会先做只读轻量探测。
4. AI 返回单条命令、多个候选，或要求澄清。
5. 用户确认后将命令发送给执行链路。
6. 后续结果会自动反馈到聊天、执行经验、长期记忆和环境画像。

这条路径已经构成“提需求 -> 选命令 -> 执行 -> 沉淀经验”的主产品体验。

### 路径 6: 命令历史复用

1. 用户通过 AI、AI 历史命令、常用命令、内置命令或主终端手动输入触发命令。
2. 命令会根据入口附带来源标记。
3. 历史按服务器维度持久化到本地。
4. 用户后续可以在 AI 面板中直接搜索、筛选并重跑历史命令。

这条路径说明项目已经开始具备“跨会话工作台”特征，而不是一次性问答型 AI 工具。

## 架构协作分析

这一节关注前端、后端和本地存储目前如何协作。

### 前端职责

前端主要承担 4 类职责：

- 界面组织：侧边栏、tab、终端、AI 面板、设置弹窗
- 应用状态：活动服务器、tab、布局、命令历史、输出映射
- 用户输入采集：表单、终端输入、AI 输入
- 通过 Tauri `invoke` 调用后端能力

当前前端的核心协调点是 `src/App.tsx`，它相当于应用层控制器。
多窗口相关逻辑则通过独立入口和窗口路由分发给文件浏览器、文件编辑器子窗口。

### 后端职责

后端主要承担 5 类职责：

- SSH 连接与命令执行
- SFTP 远程文件读写、上传下载与目录遍历
- PersistentShell 与 PTY 通道管理
- 服务器配置加密存储与解锁
- AI orchestrator、请求代理与统一响应解析
- 执行经验、长期记忆、环境画像和命令库读写

也就是说，所有真正敏感或系统级能力都在 Rust 端。

### 数据流协作

当前主要有 5 条关键数据流：

- 解锁流: UnlockScreen -> commands.rs -> crypto/storage
- 服务器流: ServerList -> commands.rs -> ssh manager + encrypted storage
- 终端流: Terminal -> shell_input/shell_output -> PersistentShell
- 文件流: 文件浏览器 / 编辑器子窗口 -> remote file commands -> SSH session + SFTP
- AI 流: AIChat -> call_ai_orchestrated -> ai.rs -> external provider -> AIChat

这几条流已经构成项目主干。

### 当前架构优点

- 前后端分工基本合理
- 敏感能力留在 Rust
- 主终端与 AI 能力已能组合工作
- 本地记忆系统和服务器环境画像已经开始反哺 AI 上下文
- 命令历史已经形成“执行 -> 持久化 -> 复用 -> 反哺 AI”的闭环

### 当前架构主要问题

- `App.tsx` 承担过多协调职责，后续会继续膨胀
- 存在 `Terminal.tsx`、`PtyTerminal.tsx`、`InteractiveTerminal.tsx` 多条终端路线并存
- 类型系统和前后端契约尚未完全收口
- PTY 终端的不可见 feedback 仍未完全收口，交互 shell 执行结果尚未稳定纳入学习闭环
- 当前命令历史仍是轻量采集，不是完整 shell transcript，交互式命令输出尚未精确关联

## 终端主线路分析

这一节专门回答“当前到底应该围绕哪套终端继续开发”。

### 当前三套终端的真实状态

#### `Terminal.tsx`

这是当前实际接入主界面的终端实现，也是当前唯一在线主路径。

它的链路是：

- `App.tsx`
- `Terminal.tsx`
- `shell_input` / `shell_output` / `shell_resize`
- `PersistentShell`

这条线路的特点是：

- 使用 xterm.js
- 按 tab 维度管理 shell 会话
- 已接入当前主布局
- AI 面板直接通过 `shell_input` 向它发命令
- 已处理 resize 和转义序列读取问题

这说明它不是候选方案，而是事实上的生产主线。

#### `PtyTerminal.tsx`

这是一套单独的 PTY 终端方案，但当前没有被 `App.tsx` 使用。

它的链路是：

- `PtyTerminal.tsx`
- `start_pty_session` / `pty_input` / `pty_output` / `pty_resize`
- `SSHConnection` 的 interactive channel

它的问题不是“不能用”，而是：

- 生命周期按 `serverId` 管理，不是按 tab 管理
- 输出轮询仍然是 `setInterval`
- 尺寸计算依赖手动字符宽高，而不是 xterm fit 后的真实 rows/cols
- 与当前主界面的 tab、AI、session 生命周期没有统一

所以它更像保留下来的备用路线或旧实验，而不是现在应该继续扩展的主线。

#### `InteractiveTerminal.tsx`

这不是一个真正的远程 PTY 终端，而是前端模拟的命令行交互视图。

它的特点是：

- 自己维护输入缓冲、光标位置和前端历史
- 依赖 `onCommandSubmit` 回调触发命令执行
- 输出显示基于 `history/currentOutput` 渲染
- 更像一个“命令控制台 UI”，不是完整终端协议实现

它适合做原型验证或简化交互界面，但不适合作为当前项目的长期主终端。

### 结论

当前应该明确：

- 保留 `Terminal.tsx + PersistentShell` 作为唯一主终端路线
- `PtyTerminal.tsx` 暂时降级为实验代码或迁移参考
- `InteractiveTerminal.tsx` 视为历史 UI 方案，不再作为主线继续扩展

### 为什么应该这样收敛

原因有四个：

1. 它已经接在当前 `App.tsx` 上，迁移成本最低。
2. 它按 `tabId` 管理 shell，会话模型更符合当前产品形态。
3. AI、布局、多 tab 这些上层功能已经默认依赖 `shell_input` 这套接口。
4. 你已经在这条线上修过转义序列和轮询竞态问题，经验已经沉淀在代码里。

### 建议的迁移原则

后续终端相关开发统一遵守这几个原则：

1. 所有主终端输入输出都走 `shell_*` 接口。
2. 会话生命周期统一以 `tabId` 为主键，而不是 `serverId`。
3. 行列同步统一以 xterm 的真实 `rows/cols` 为准，不再自己估算字符尺寸。
4. 输出读取统一使用顺序轮询，不再新增 `setInterval` 型并发轮询。
5. 命令历史优先通过统一入口写入，避免 AI、终端、侧边栏各自分叉记录。

### 建议的代码收敛顺序

#### 第一步

在文档和注释层面正式声明 `Terminal.tsx` 为主终端实现。

#### 第二步

把 `PtyTerminal.tsx` 标记为 legacy / experimental，避免后续误用。

#### 第三步

梳理哪些交互能力还只存在于 `PtyTerminal.tsx`，例如：

- 某些快捷键处理
- 右键粘贴
- 选中自动复制

有价值的能力迁回 `Terminal.tsx`，而不是继续双线维护。

#### 第四步

确认 `InteractiveTerminal.tsx` 是否还需要保留：

- 如果只用于历史参考，可以保留但不继续扩展
- 如果没有明确用途，可以后续移出主仓库实现目录

## 接下来如何继续开发更合理

建议按照下面顺序推进，而不是继续平铺功能。

### 第一阶段: 先收敛主干

这一阶段已基本完成：

1. 前端 TypeScript 构建已恢复通过
2. `src/types/index.ts` 与主要前后端契约已收敛一轮
3. 主终端已明确围绕 `Terminal.tsx + PersistentShell` 继续开发

理由：

- 如果没有这一阶段的收敛，后面的历史、AI、会话和多主机能力都会继续分叉
- 当前仍要持续做小规模的契约收口，但主干已经从“容易断”进入“可以迭代”

### 第二阶段: 统一终端与会话模型

这一阶段已完成主方向判断，并完成了主线增强：

1. 已明确 `Terminal.tsx` 为主实现
2. 备用终端路线已降级为 legacy / experimental
3. 主终端已补上基础交互增强，并将长时间后台切回、长时间前台空闲、连接类错误统一收口到“手动重连提示”
4. 新增终端相关功能优先走 `shell_*` 主路径
5. 已修复多 tab 切换时误清理 shell session、后台 tab 干扰前台会话导致的断连、卡顿与欢迎信息重复问题
6. 已修复终端舞台层叠命中问题，避免后开的 tab 覆盖先开的 tab 导致“看起来像卡死”
7. 重连动作现在会先清屏再重建会话，减少旧失效画面和新会话内容混杂

理由：

- 这是整个产品体验的地基
- 终端路线长期并存会拖累所有上层能力，包括 AI、历史、状态同步

### 第三阶段: 补齐持久化工作台能力

这一阶段已开始推进，当前状态如下：

1. 已完成按服务器维度的命令执行结果持久化
2. 已完成跨会话命令历史的基本复用
3. 已支持 AI、终端手动输入、历史重跑、常用命令、内置命令的来源标记
4. 已支持历史去重、搜索、来源筛选、裁剪和清空
5. 下一步重点应放在会话恢复、命令输出搜索与回放

理由：

- 这些能力会把产品从“会话工具”推向“工作台工具”
- 它们和已经存在的 memory system 天然兼容

### 服务器状态采集解耦

左侧服务器状态现在不再复用主终端相关的 SSH 会话，也不再依赖 `execute_command + session_pool` 这条共享链路。

当前实现改为：

1. 通过独立的 `get_server_status` 后端命令获取状态
2. 每次采集使用一条独立短连接
3. 在同一条短连接里顺序执行磁盘、内存、网络状态采样
4. 采集完成后立即断开

这样做的原因是：

- 服务器状态面板属于只读采集，不应该和主终端的持久 shell 争用 SSH 资源
- 主终端负责交互式 shell，会话应尽量稳定保留
- 左侧状态刷新应当对终端输入输出透明

这次拆分之后，终端交互和状态同步的职责边界更清楚，也更符合长期可维护的架构。

### 第四阶段: 再扩展多主机与高级能力

最后再推进：

1. 服务器分组 / 标签
2. 批量执行
3. 多主机同时执行
4. 导出 / 报告 / 计划任务 / 监控告警

理由：

- 这些功能价值高，但复杂度也高
- 它们依赖稳定的终端模型、统一的执行模型和可靠的结果持久化

## 正在推进的方向

### 高优先级

- [ ] SSH 会话保持与复用进一步稳定化
- [ ] 会话恢复与断线恢复体验继续完善
- [ ] 命令输出搜索与回放
- [ ] 服务器分组 / 标签管理
- [ ] 批量命令执行

### 中优先级

- [ ] PTY 会话复用与统一
- [ ] 命令输出搜索 / 过滤
- [ ] 快捷命令 / 命令别名
- [ ] 多主机同时执行
- [ ] 命令输出导出
- [ ] 深色 / 浅色主题切换

### 低优先级

- [ ] SSH 密钥 passphrase 缓存
- [ ] 命令执行超时设置
- [ ] 自定义提示符格式
- [ ] 命令执行计划任务
- [ ] 服务器监控告警规则

## 开发命令

### 前端

```bash
npm install
npm run dev
npm run build
npm run lint
```

### Tauri

```bash
npm run tauri:dev
npm run tauri:build
```

### Rust

```bash
cd src-tauri
cargo check
cargo build
cargo test
```

## 项目结构

```text
LazyShell/
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── components/
│   │   ├── AIChat.tsx
│   │   ├── Terminal.tsx
│   │   ├── PtyTerminal.tsx
│   │   ├── InteractiveTerminal.tsx
│   │   ├── ServerList.tsx
│   │   ├── ServerStatus.tsx
│   │   ├── TabBar.tsx
│   │   ├── ResizablePanels.tsx
│   │   ├── Settings.tsx
│   │   ├── ProviderSelector.tsx
│   │   ├── AddProviderModal.tsx
│   │   └── UnlockScreen.tsx
│   ├── hooks/
│   │   ├── useCommandDatabase.ts
│   │   └── useMemory.ts
│   ├── providers/
│   │   └── aiProvider.ts
│   └── types/
│       └── index.ts
├── src-tauri/
│   └── src/
│       ├── main.rs
│       ├── lib.rs
│       ├── commands.rs
│       ├── ssh.rs
│       ├── ai.rs
│       ├── crypto.rs
│       ├── learning.rs
│       ├── memory.rs
│       ├── commands_db.rs
│       └── commands_db.json
├── docs/
└── package.json
```

## 关键模块

### 前端

- `src/App.tsx`: 主状态中枢，负责服务器、标签、布局、终端会话和顶层交互
- `src/components/Terminal.tsx`: 当前主终端实现
- `src/components/AIChat.tsx`: AI 对话、候选命令、记忆接入
- `src/components/RemoteFileManager.tsx`: 远程文件浏览器子窗口
- `src/components/FileEditorWindow.tsx`: 远程文本编辑器子窗口
- `src/components/ServerList.tsx`: 服务器管理入口
- `src/components/TabBar.tsx`: 多标签切换
- `src/hooks/useMemory.ts`: 聊天历史和命令卡片访问层
- `src/providers/aiProvider.ts`: AI Provider 抽象

### 后端

- `src-tauri/src/commands.rs`: Tauri IPC 命令注册与应用主操作入口
- `src-tauri/src/ssh.rs`: SSH 连接、命令执行、PersistentShell、PTY 相关能力
- `src-tauri/src/crypto.rs`: 本地加密存储
- `src-tauri/src/ai.rs`: AI orchestrator、环境探测与 Provider 调用
- `src-tauri/src/learning.rs`: 执行经验、长期记忆、服务器环境画像
- `src-tauri/src/memory.rs`: 聊天历史、命令卡片、危险级别判断
- `src-tauri/src/commands_db.rs`: 内置命令数据库

## 重要架构说明

### 1. 终端实现分层

当前主流程使用 `Terminal.tsx` 对接后端 `PersistentShell`。仓库中还保留了 `PtyTerminal.tsx` 与 PTY 相关接口，但它不是当前 `App.tsx` 的主线路。

可以简单理解为：

- `Terminal.tsx` 是当前主终端实现
- `PtyTerminal.tsx` 是备用 / 试验性 PTY 路线
- `InteractiveTerminal.tsx` 是另一套交互终端实现

后续开发需要优先统一这几条终端线路，避免长期并存。

### 2. 终端轮询约束

主终端读取输出时使用顺序轮询，而不是并发定时器轮询。这个约束很关键，尤其在 `vim`、`nano` 等全屏交互程序中，如果改成并发轮询，容易出现转义序列乱序和界面异常。

### 3. AI 上下文注入

AI 请求当前会注入这些上下文：

- 当前工作目录
- 最近命令及其输出摘要
- 当前服务器会话状态
- 本地记忆内容，例如常用命令和近期对话摘要
- 执行经验与长期记忆
- 服务器环境画像，必要时会通过只读 probe 补充能力标签

### 4. 安全边界

所有远程命令都应遵守以下原则：

- 默认可预览
- 高危命令必须二次确认
- 用户主密码仅用于本地加密解锁
- 私钥和密码只做本地加密存储，不上传第三方

## 状态管理说明

当前以前端原生 Hooks 为主：

- 本地状态: `useState`
- 回调缓存: `useCallback`
- 派生数据: `useMemo`
- 跨组件共享: 以 `App.tsx` 状态提升为主

这套方式在当前阶段可用，但随着功能增加，需要警惕 `App.tsx` 继续膨胀和 props drilling 加深。

## 开发约定

### TypeScript

- 优先函数式组件
- 组件 Props 在组件上方定义
- `useEffect` 必须有明确依赖
- 事件处理函数优先使用稳定引用
- 类型定义优先收口到 `src/types/index.ts`

### Rust

- 使用 `Result<T, E>` 进行错误传播
- 错误转换优先 `map_err`
- 共享状态通过 `Arc<Mutex<T>>` 管理
- 避免在锁内执行明显耗时操作

## 安全实现

- 配置加密算法: AES-256-GCM
- 密钥派生: PBKDF2
- 加密对象: 服务器密码、私钥、配置数据
- 主密码: 仅在本地会话期间驻留内存

## 适合作为下一步的开发方向

如果继续在 Codex 上推进，这个项目下一阶段最适合优先做这三件事：

1. 收敛前端类型系统和 Provider / Memory 契约
2. 统一终端实现路线，明确 `Terminal` / `PtyTerminal` / `InteractiveTerminal` 的主次关系
3. 补齐会话保持、命令结果持久化和跨会话历史
